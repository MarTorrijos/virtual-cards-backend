spring:
  datasource:
    url: jdbc:mysql://localhost:3306/virtual_cards
    # change these for your credentials
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

jwt:
  secret: someSuperLongAndRandomSecretKey
